# Server-side base URL used by Next.js rewrites (/api/* -> BFF).
BFF_BASE_URL=http://localhost:8001

# Optional: override for client-side direct calls. Leave empty to use same-origin /api.
# NEXT_PUBLIC_BFF_BASE_URL=http://localhost:8001
